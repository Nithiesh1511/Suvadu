# Admin portal credentials.
# Copy this file to ".env.local" and set your own values.
# NOTE: Vite inlines these into the client bundle at build time — this is a
# prototype gate, not real (server-side) authentication.
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=suvadu-admin
